# Generated by Flutter
# To build an app, use `flutter build ios`.

# The next line is required for Flutter
require File.expand_path(File.join('..', 'Flutter', 'podhelper.rb'), __FILE__)

platform :ios, '15.0'

# CocoaPods analytics sends network stats synchronous to help focus development efforts on most used plugins.
# If you would like to disable this, uncomment the following line.
# install! 'cocoapods', :deterministic_uuids => false

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    
    # Ensure all targets use iOS 15.0 to avoid conflicts with 'cloud_firestore'
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
    end
  end
end
